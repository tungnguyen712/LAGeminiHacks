import type { Segment } from '../../types/Segment';

interface Props {
  segment: Segment;
}

// TODO: render colored polyline on map
export function SegmentPolyline({ segment }: Props) {
  return null;
}
