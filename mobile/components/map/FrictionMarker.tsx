import type { Segment } from '../../types/Route';

interface Props {
  segment: Segment;
  onPress: () => void;
}

// TODO: render friction marker on map
export function FrictionMarker({ segment, onPress }: Props) {
  return null;
}
